(function (define) {
    'use strict';

    define(['step'], function (Step) {
        var step = new Step(3, 'work-tab', {
            
        }, {
            
        }, {
            init: function () {
                
            },
            validate: function () {
                
            }
        });

        return step;
    });
}(window.define));